import { CATEGORY_SORT_BY } from './category-sort-by';

import { ORDER } from '../../common/constants/order';

export const CATEGORY_DEFAULTS = {
	LIMIT: 10,
	SORT_BY: CATEGORY_SORT_BY.NAME,
	ORDER: ORDER.ASC,
};
